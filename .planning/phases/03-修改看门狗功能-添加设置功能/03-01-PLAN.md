---
phase: 03-修改看门狗功能-添加设置功能
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/lively_config.json
  - bean/beans.py
  - api.py
autonomous: true
requirements:
  - UI-WATCHDOG-03
  - UI-WATCHDOG-04
  - UI-WATCHDOG-05
user_setup: []

must_haves:
  truths:
    - "Config file has rtf_threshold and watchdog_auto_recovery keys"
    - "WatchdogTask model exists and creates watchdog_tasks table"
    - "Settings API accepts and returns new watchdog config fields"
  artifacts:
    - path: "config/lively_config.json"
      provides: "Watchdog configuration storage"
      contains: "rtf_threshold, watchdog_auto_recovery"
    - path: "bean/beans.py"
      provides: "WatchdogTask database model"
      contains: "class WatchdogTask"
    - path: "api.py"
      provides: "Settings API extension"
      contains: "rtf_threshold, watchdog_auto_recovery in SettingsRequest"
  key_links:
    - from: "api.py"
      to: "config/lively_config.json"
      via: "save_settings endpoint"
    - from: "bean/beans.py"
      to: "db"
      via: "WatchdogTask model"
---

<objective>
Add watchdog configuration keys to config file, create WatchdogTask database model, and extend SettingsRequest/Settings API to handle new watchdog fields.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@config/lively_config.json
@bean/beans.py
@api.py (lines 1780-1865)
</context>

<interfaces>
<!-- WatchdogTask model that will be created -->
class WatchdogTask(BaseModel):
    id = AutoField(primary_key=True)
    job_id = CharField(max_length=100, unique=True)
    novel_name = CharField(max_length=100)
    thread_count = IntegerField(default=4)
    total_chapters = IntegerField()
    completed_chapters = IntegerField(default=0)
    is_running = BooleanField(default=False)
    create_time = DateTimeField(default=datetime.datetime.now)
    update_time = DateTimeField(default=datetime.datetime.now)

<!-- SettingsRequest fields to add -->
class SettingsRequest(BaseModel):
    rtf_threshold: Optional[float] = None
    watchdog_auto_recovery: Optional[bool] = None
    watchdog_reload_wait_seconds: Optional[int] = None
    watchdog_resume_wait_seconds: Optional[int] = None
    watchdog_log_check_lines: Optional[int] = None
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add watchdog config keys to lively_config.json</name>
  <files>config/lively_config.json</files>
  <read_first>config/lively_config.json</read_first>
  <action>
    Add the following new keys to config/lively_config.json (maintain existing keys):
    {
      "rtf_threshold": 0.8,
      "watchdog_auto_recovery": true,
      "watchdog_reload_wait_seconds": 60,
      "watchdog_resume_wait_seconds": 120,
      "watchdog_log_check_lines": 10
    }
  </action>
  <verify>
    <automated>grep -c "rtf_threshold" config/lively_config.json && grep -c "watchdog_auto_recovery" config/lively_config.json</automated>
  </verify>
  <done>config/lively_config.json contains rtf_threshold: 0.8, watchdog_auto_recovery: true, watchdog_reload_wait_seconds: 60, watchdog_resume_wait_seconds: 120, watchdog_log_check_lines: 10</done>
</task>

<task type="auto">
  <name>Task 2: Create WatchdogTask model in beans.py</name>
  <files>bean/beans.py</files>
  <read_first>bean/beans.py</read_first>
  <action>
    Add WatchdogTask model to bean/beans.py after ScheduledTask class (before db.create_tables line).

    Model structure:
    ```python
    class WatchdogTask(BaseModel):
        id = AutoField(primary_key=True)
        job_id = CharField(max_length=100, unique=True)
        novel_name = CharField(max_length=100)
        thread_count = IntegerField(default=4)
        total_chapters = IntegerField()
        completed_chapters = IntegerField(default=0)
        is_running = BooleanField(default=False)
        create_time = DateTimeField(default=datetime.datetime.now)
        update_time = DateTimeField(default=datetime.datetime.now)

        class Meta:
            table_name = 'watchdog_tasks'
    ```

    Then update db.create_tables line to include WatchdogTask:
    `db.create_tables([Novel,Role,NovelName,RoleAudio,ScheduledTask,WatchdogTask])`
  </action>
  <verify>
    <automated>grep -c "class WatchdogTask" bean/beans.py && grep -c "watchdog_tasks" bean/beans.py</automated>
  </verify>
  <done>WatchdogTask model defined with table_name='watchdog_tasks', all fields present, table creation updated</done>
</task>

<task type="auto">
  <name>Task 3: Extend SettingsRequest and get_default_settings in api.py</name>
  <files>api.py</files>
  <read_first>api.py (lines 1780-1865)</read_first>
  <action>
    In api.py, modify SettingsRequest (around line 1786) to add these Optional fields:
    - rtf_threshold: Optional[float] = None
    - watchdog_auto_recovery: Optional[bool] = None
    - watchdog_reload_wait_seconds: Optional[int] = None
    - watchdog_resume_wait_seconds: Optional[int] = None
    - watchdog_log_check_lines: Optional[int] = None

    Then modify get_default_settings() (around line 1798) to include:
    - "rtf_threshold": 0.8
    - "watchdog_auto_recovery": True
    - "watchdog_reload_wait_seconds": 60
    - "watchdog_resume_wait_seconds": 120
    - "watchdog_log_check_lines": 10
  </action>
  <verify>
    <automated>grep -c "rtf_threshold.*Optional\[float\]" api.py && grep -c "watchdog_auto_recovery.*Optional\[bool\]" api.py</automated>
  </verify>
  <done>SettingsRequest accepts rtf_threshold, watchdog_auto_recovery, watchdog_reload_wait_seconds, watchdog_resume_wait_seconds, watchdog_log_check_lines. get_default_settings returns these with correct defaults.</done>
</task>

</tasks>

<verification>
- config/lively_config.json readable JSON with new keys
- WatchdogTask model imports work without error
- API settings endpoint accepts and returns new fields
</verification>

<success_criteria>
1. config/lively_config.json has rtf_threshold, watchdog_auto_recovery, watchdog_reload_wait_seconds, watchdog_resume_wait_seconds, watchdog_log_check_lines
2. WatchdogTask model creates watchdog_tasks table on startup
3. SettingsRequest in api.py has new fields
4. get_default_settings returns correct defaults
</success_criteria>

<output>
After completion, create `.planning/phases/03-修改看门狗功能-添加设置功能/03-01-SUMMARY.md`
</output>
