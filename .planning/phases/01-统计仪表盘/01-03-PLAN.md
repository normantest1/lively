---
phase: 01-统计仪表盘
plan: "03"
type: execute
wave: 2
depends_on:
  - "01"
  - "02"
files_modified:
  - admin/src/views/DashboardView.vue
  - admin/src/router/index.js
  - admin/src/App.vue
autonomous: true
requirements:
  - UI-STATS-01
must_haves:
  truths:
    - "Dashboard displays 4 stat cards (novels, roles, audios, pending)"
    - "Dashboard displays chapter statistics"
    - "Dashboard displays top 50 hot roles table"
    - "Dashboard displays novel distribution chart"
    - "Dashboard has responsive grid layout"
  artifacts:
    - path: "admin/src/views/DashboardView.vue"
      provides: "Dashboard component"
      min_lines: 200
      contains: "el-card"
      contains: "el-chart"
      contains: "el-table"
    - path: "admin/src/router/index.js"
      provides: "Dashboard route"
      contains: "/dashboard"
    - path: "admin/src/App.vue"
      provides: "Dashboard menu entry"
      contains: "dashboard"
  key_links:
    - from: "DashboardView.vue"
      to: "admin/src/api/index.js"
      via: "getStatsOverview, getStatsNovels, getStatsTopRoles, getStatsPending"
    - from: "admin/src/App.vue"
      to: "admin/src/router/index.js"
      via: "el-menu-item index=/dashboard"
---

<objective>
Create the statistics dashboard Vue component with cards, charts, and tables.
</objective>

<context>
Per D-13, D-14, D-16:
- Component name: DashboardView.vue
- Location: admin/src/views/DashboardView.vue
- Use <script setup> syntax

Per D-01, D-02, D-03, D-04:
- Card grid layout using el-row and el-col
- 4 top stat cards: 小说总数, 角色总数, 音频总数, 待处理
- Middle charts area: bar chart for novel distribution, pie chart for role distribution
- Bottom: table for top 50 hot roles

API functions available (from plans 01 and 02):
- getStatsOverview(): {total_novels, total_chapters, total_roles, total_audios}
- getStatsPending(): {unparsed_novels, ungenerated_chapters}
- getStatsNovels(): {novels: [{id, novel_name, chapter_count, role_count, current_state, state_name}]}
- getStatsTopRoles(limit): {roles: [{id, role_name, novel_name, role_count, gender, is_bind, presence_rate}]}

Existing Vue pattern from NovelManage.vue:
- el-card for containers
- el-table for data display
- el-button for actions
- format functions for date/time
- loading state with v-loading
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DashboardView.vue with stat cards</name>
  <files>admin/src/views/DashboardView.vue</files>
  <action>
Create DashboardView.vue with:

1. Script setup section:
   - Import ref, onMounted from vue
   - Import api methods: getStatsOverview, getStatsPending, getStatsNovels, getStatsTopRoles from '@/api'
   - Create reactive state: overview ({}), pending ({}), novels ([]), topRoles ([]), loading (false)
   - onMounted: fetch all 4 data sources in parallel using Promise.all

2. Template structure (per D-01, D-02, D-03, D-04):
   - el-row with 4 el-col (span=6 each) for stat cards:
     * 小说总数: overview.total_novels
     * 角色总数: overview.total_roles
     * 音频总数: overview.total_audios
     * 待处理: pending.unparsed_novels + pending.ungenerated_chapters
   - el-row with 2 el-col for charts:
     * el-chart (bar): novel distribution by state
     * el-chart (pie): role gender distribution
   - el-table for top 50 roles: columns (角色名, 小说名, 引用次数, 性别, 绑定状态, 出场率)
   - Refresh button to reload data

3. Style section with basic card styling.

Follow Element Plus patterns from existing views. Use el-tag for state display.
</action>
  <verify>
  <automated>grep -c "el-card\|el-chart\|el-table" admin/src/views/DashboardView.vue</automated>
  </verify>
  <done>DashboardView.vue created with stat cards, charts, and roles table using <script setup></done>
</task>

<task type="auto">
  <name>Task 2: Add dashboard route to router</name>
  <files>admin/src/router/index.js</files>
  <action>
Add to routes array:
```javascript
{
  path: '/dashboard',
  name: 'DashboardView',
  component: () => import('@/views/DashboardView.vue'),
  meta: { title: '统计仪表盘' }
}
```

Also add import at top:
```javascript
// Remove component import, use lazy loading
```

Add after the existing '/novels' route, before '/novel-names'.
</action>
  <verify>
  <automated>grep -c "/dashboard" admin/src/router/index.js</automated>
  </verify>
  <done>Router has /dashboard route pointing to DashboardView component</done>
</task>

<task type="auto">
  <name>Task 3: Add dashboard menu item to App.vue</name>
  <files>admin/src/App.vue</files>
  <action>
Add as FIRST el-menu-item in the el-menu (before /novels):
```html
<el-menu-item index="/dashboard">
  <span>统计仪表盘</span>
</el-menu-item>
```

Set the redirect in router from '/' to '/dashboard' instead of '/novels'.
</action>
  <verify>
  <automated>grep -c "统计仪表盘" admin/src/App.vue</automated>
  </verify>
  <done>App.vue menu shows 统计仪表盘 as first entry, clicking redirects to dashboard</done>
</task>

</tasks>

<verification>
- DashboardView.vue exists at admin/src/views/DashboardView.vue with el-card, el-chart, el-table
- Router has /dashboard route
- App.vue has menu item for dashboard
</verification>

<success_criteria>
Dashboard page loads and displays all stat cards, charts, and roles table with data fetched from APIs.
</success_criteria>

<output>
After completion, create `.planning/phases/01-统计仪表盘/01-03-SUMMARY.md`
</output>
