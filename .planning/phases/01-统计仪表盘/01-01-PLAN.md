---
phase: 01-统计仪表盘
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - api.py
  - admin/src/api/index.js
autonomous: true
requirements:
  - UI-STATS-02
must_haves:
  truths:
    - "Dashboard can fetch global overview statistics"
    - "Dashboard can fetch pending work statistics"
  artifacts:
    - path: "api.py"
      provides: "Backend API endpoints"
      contains: "/api/stats/overview"
      contains: "/api/stats/pending"
    - path: "admin/src/api/index.js"
      provides: "Frontend API calls"
      exports:
        - "getStatsOverview"
        - "getStatsPending"
  key_links:
    - from: "admin/src/api/index.js"
      to: "api.py /api/stats/overview"
      via: "axios GET"
    - from: "admin/src/api/index.js"
      to: "api.py /api/stats/pending"
      via: "axios GET"
---

<objective>
Create backend API endpoints for statistics overview and pending counts.
</objective>

<context>
From bean/beans.py - Database models:
- Novel: id, chapter_names, section_data_json, after_analysis_data_json, create_time, novel_name, current_state
- Role: id, novel_name, role_name, role_count, gender, is_bind, bind_audio_name, chapter_count, presence_rate
- RoleAudio: id, role_name, audio_path, gender, audio_text, citation_count, audio_uri

Existing pattern in api.py (lines 1262-1298):
```python
@app.get("/api/statistics/novels-summary")
def get_novels_summary():
    total_novels = Novel.select().count()
    ...
```

current_state values: 1=已分片待解析, 2=已解析待合成, 3=已合成语音
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /api/stats/overview endpoint</name>
  <files>api.py</files>
  <action>
Add GET /api/stats/overview endpoint returning:
- total_novels: count of all Novel records
- total_chapters: sum of all chapter counts across novels (count commas in chapter_names + 1 per novel)
- total_roles: count of all Role records
- total_audios: count of all RoleAudio records

Follow existing API pattern from /api/statistics/novels-summary (lines 1262-1276).
Return JSON response with these fields.
  </action>
  <verify>
  <automated>curl -s http://localhost:port/api/stats/overview | python -c "import sys,json; d=json.load(sys.stdin); assert 'total_novels' in d and 'total_roles' in d and 'total_audios' in d; print('OK')"</automated>
  </verify>
  <done>GET /api/stats/overview returns JSON with total_novels, total_chapters, total_roles, total_audios</done>
</task>

<task type="auto">
  <name>Task 2: Create /api/stats/pending endpoint</name>
  <files>api.py</files>
  <action>
Add GET /api/stats/pending endpoint returning:
- unparsed_novels: count of Novel records where current_state == 1
- ungenerated_chapters: for novels where current_state in [1,2], count chapters not yet synthesized

Calculate ungenerated_chapters by summing chapters that haven't reached state 3.
Return JSON response with these fields.
  </action>
  <verify>
  <automated>curl -s http://localhost:port/api/stats/pending | python -c "import sys,json; d=json.load(sys.stdin); assert 'unparsed_novels' in d and 'ungenerated_chapters' in d; print('OK')"</automated>
  </verify>
  <done>GET /api/stats/pending returns JSON with unparsed_novels and ungenerated_chapters counts</done>
</task>

<task type="auto">
  <name>Task 3: Add frontend API functions for stats</name>
  <files>admin/src/api/index.js</files>
  <action>
Add to apiClient export two new functions:
- getStatsOverview(): calls GET /api/stats/overview
- getStatsPending(): calls GET /api/stats/pending

Follow existing pattern from getNovelsSummary() and getRolesSummary() (lines 151-157).
</action>
  <verify>
  <automated>grep -c "getStatsOverview\|getStatsPending" admin/src/api/index.js</automated>
  </verify>
  <done>getStatsOverview() and getStatsPending() exported from admin/src/api/index.js</done>
</task>

</tasks>

<verification>
- api.py contains @app.get("/api/stats/overview") and @app.get("/api/stats/pending")
- admin/src/api/index.js exports getStatsOverview and getStatsPending
</verification>

<success_criteria>
Both /api/stats/overview and /api/stats/pending return valid JSON with required fields.
</success_criteria>

<output>
After completion, create `.planning/phases/01-统计仪表盘/01-01-SUMMARY.md`
</output>
